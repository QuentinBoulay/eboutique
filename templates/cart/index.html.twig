{% extends 'base.html.twig' %}

{% block title %}Mon panier{% endblock %}

{% block body %}
    <style>
    </style>

    <div class="page-content container">
        <h1>Mon panier</h1>

        {% if basket %}
            {{ dump(basket) }}
            {{ dump(basketDetails) }}
        {% endif %}
        
    </div>
{% endblock %}
